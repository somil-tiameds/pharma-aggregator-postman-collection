$kind: http-request
url: "{{baseUrl}}/temp-sellers/coordinator/check-phone?mobile={{phone}}&tempSellerId={{tempSellerId}}"
method: GET
queryParams:
  mobile: "{{phone}}"
  tempSellerId: "{{tempSellerId}}"
order: 2000
