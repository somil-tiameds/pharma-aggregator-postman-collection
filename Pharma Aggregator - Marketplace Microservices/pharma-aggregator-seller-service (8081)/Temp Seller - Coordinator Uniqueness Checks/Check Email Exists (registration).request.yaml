$kind: http-request
url: "{{baseUrl}}/temp-sellers/coordinator/check-email?email={{email}}&tempSellerId={{tempSellerId}}"
method: GET
queryParams:
  email: "{{email}}"
  tempSellerId: "{{tempSellerId}}"
order: 1000
