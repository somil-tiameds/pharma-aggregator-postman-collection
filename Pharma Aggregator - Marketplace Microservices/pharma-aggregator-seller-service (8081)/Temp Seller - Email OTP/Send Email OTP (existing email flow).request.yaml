$kind: http-request
url: "{{baseUrl}}/temp-seller/email-otp/exist/send"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "email": "{{email}}"
    }
order: 2000
