$kind: http-request
url: "{{baseUrl}}/lab-admin/stats/{{labId}}/earnings-by-category?startDate={{startDate}}&endDate={{endDate}}"
method: GET
headers:
  Authorization: Bearer {{accessToken}}
queryParams:
  startDate: "{{startDate}}"
  endDate: "{{endDate}}"
settings: {}
order: 13000
