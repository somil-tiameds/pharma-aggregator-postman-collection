$kind: http-request
url: "{{baseUrl}}/lab-admin/stats/{{labId}}/sample-workflow-funnel?startDate={{startDate}}&endDate={{endDate}}"
method: GET
headers:
  Authorization: Bearer {{accessToken}}
queryParams:
  startDate: "{{startDate}}"
  endDate: "{{endDate}}"
settings: {}
order: 24000
