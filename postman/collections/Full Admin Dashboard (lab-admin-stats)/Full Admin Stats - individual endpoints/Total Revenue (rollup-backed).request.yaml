$kind: http-request
url: "{{baseUrl}}/lab-admin/stats/{{labId}}/total-revenue?startDate={{startDate}}&endDate={{endDate}}"
method: GET
headers:
  Authorization: Bearer {{accessToken}}
queryParams:
  startDate: "{{startDate}}"
  endDate: "{{endDate}}"
settings: {}
order: 6000
