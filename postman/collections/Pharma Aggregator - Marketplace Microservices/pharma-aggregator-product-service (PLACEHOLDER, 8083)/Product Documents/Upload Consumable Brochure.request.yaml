$kind: http-request
description: Replaces the brochurePath on an existing consumable medical attribute.
url: "{{baseUrl}}/product-documents/consumable/{{productAttributeId}}/brochure?uploadedBy={{sellerId}}"
method: POST
body:
  type: formdata
  content:
    - type: file
      key: brochureFile
      src: ""
      description: Brochure document (PDF)
order: 4000
