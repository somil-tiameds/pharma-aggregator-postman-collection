$kind: http-request
description: Returns true if the batch number is NOT already used by this seller for this category (i.e. it's free to use for a new batch).
url: "{{baseUrl}}/pricing/validateBatchNumber?batchNumber={{batchLotNumber}}&sellerId={{sellerId}}&categoryId={{categoryId}}"
method: GET
order: 1000
