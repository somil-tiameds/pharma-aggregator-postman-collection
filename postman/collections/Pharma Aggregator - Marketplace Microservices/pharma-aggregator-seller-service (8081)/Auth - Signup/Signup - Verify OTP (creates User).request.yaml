$kind: http-request
description: Verifies the signup OTP and creates the tbl_user row.
url: "{{baseUrl}}/auth/signup/verify-otp"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "email": "{{email}}",
      "otp": "123456"
    }
order: 2000
