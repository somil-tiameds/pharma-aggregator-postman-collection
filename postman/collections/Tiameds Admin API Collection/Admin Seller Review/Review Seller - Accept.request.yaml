$kind: http-request
description: Accept seller registration after review
url: "{{baseUrl}}/admin/sellers/review"
method: POST
headers:
  Authorization: Bearer {{authToken}}
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "id": {{pendingSellerId}},
      "status": "ACCEPT",
      "comments": "All documents verified successfully"
    }
scripts:
  - type: afterResponse
    code: |-
      if (pm.response.code === 200) {
          pm.test('Seller review processed', function() {
              pm.expect(pm.response.status).to.equal('OK');
          });
      }
    language: text/javascript
settings: {}
order: 1000
