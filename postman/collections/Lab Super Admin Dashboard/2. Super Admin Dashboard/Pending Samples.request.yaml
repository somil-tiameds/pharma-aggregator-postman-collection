$kind: http-request
url: "{{baseUrl}}/api/v1/lab-super-admin/stats/pending-samples?startDate={{startDate}}&endDate={{endDate}}"
method: GET
headers:
  Authorization: Bearer {{token}}
queryParams:
  startDate: "{{startDate}}"
  endDate: "{{endDate}}"
settings: {}
order: 11000
