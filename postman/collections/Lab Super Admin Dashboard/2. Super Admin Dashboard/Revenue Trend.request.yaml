$kind: http-request
description: startDate/endDate are required for this endpoint (no all-time variant).
url: "{{baseUrl}}/api/v1/lab-super-admin/stats/revenue-trend?startDate={{startDate}}&endDate={{endDate}}"
method: GET
headers:
  Authorization: Bearer {{token}}
queryParams:
  startDate: "{{startDate}}"
  endDate: "{{endDate}}"
settings: {}
order: 13000
