$kind: http-request
url: "{{baseUrl}}/api/v1/lab-super-admin/stats/top-referring-doctors?startDate={{startDate}}&endDate={{endDate}}&limit={{limit}}"
method: GET
headers:
  Authorization: Bearer {{token}}
queryParams:
  startDate: "{{startDate}}"
  endDate: "{{endDate}}"
  limit: "{{limit}}"
settings: {}
order: 16000
