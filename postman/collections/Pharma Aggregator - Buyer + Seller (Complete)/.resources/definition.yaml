$kind: collection
description: |-
  Every backend endpoint touching both the buyer and seller modules, plus the shared master data they both use. Every path, request body field, and query param below was read directly from the actual controller/DTO source files, not reconstructed from memory. Base path already includes the app's /api/v1 context-path.

  Known gaps (do NOT exist on the backend, so intentionally not included): buyer pending-only filter, buyer status counts/stats, a listing endpoint for real approved Buyer rows, buyer delivery-address CRUD, buyer post-approval profile edit, checkout-time approval gate.
variables:
  baseUrl: http://localhost:8080/api/v1
  buyerUsername: somilm@tiameds.ai
  buyerUserId: "1"
  tempBuyerId: "2"
  buyerDocumentId: "2"
  buyerAccessToken: ""
  buyerRefreshToken: ""
  sellerUsername: seller@example.com
  sellerUserId: "1"
  sellerId: SEHOS0001
  tempSellerId: "1"
  sellerDocumentId: "1"
  pendingSellerId: "1"
  sellerAccessToken: ""
  sellerRefreshToken: ""
  resetToken: ""
