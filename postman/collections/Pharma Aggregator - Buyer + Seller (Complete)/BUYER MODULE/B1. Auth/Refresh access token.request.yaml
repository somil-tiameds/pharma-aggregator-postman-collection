$kind: http-request
url: "{{baseUrl}}/buyer/authentication/refresh"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "refreshToken": "{{buyerRefreshToken}}"
    }
settings: {}
order: 5000
