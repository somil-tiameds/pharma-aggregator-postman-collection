$kind: http-request
url: "{{baseUrl}}/auth/validate-reset-token"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "token": "{{passwordResetToken}}"
    }
settings: {}
order: 4000
