$kind: http-request
url: "{{baseUrl}}/admin/seller-requests/batch-approve?approvedBy={{approvedBy}}"
method: POST
headers:
  Content-Type: application/json
queryParams:
  approvedBy: "{{approvedBy}}"
body:
  type: text
  content: "[{{pendingSellerId}}]"
settings: {}
order: 6000
