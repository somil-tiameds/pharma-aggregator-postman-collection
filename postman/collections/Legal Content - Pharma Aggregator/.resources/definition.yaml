$kind: collection
description: Endpoints for tbl_legal_content (Terms & Conditions / declarations served to the buyer and seller registration flows). Base path already includes /api/v1 via {{baseUrl}}.
variables:
  baseUrl: http://localhost:8080/api/v1
