$kind: http-example
request:
  url: "{{baseUrl}}/auth/logout"
  method: POST
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: |-
      {
        "status": "success",
        "message": "Logged out successfully",
        "data": { "tokenVersion": 2 }
      }
order: 1000
