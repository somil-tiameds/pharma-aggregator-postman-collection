$kind: http-request
description: Returns a molecule's details, including strength-format options scoped to the given product attribute.
url: "{{baseUrl}}/molecules/getMoleculeById?moleculeId=1&productAttributeId=PA-0001"
method: GET
queryParams:
  moleculeId: "1"
  productAttributeId: PA-0001
auth:
  type: noauth
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status code is successful', function () {
          pm.expect(pm.response.code).to.be.oneOf([200, 201, 202, 204]);
      });

      pm.test('Response time is acceptable', function () {
          pm.expect(pm.response.responseTime).to.be.below(5000);
      });
    language: text/javascript
settings: {}
order: 29000
