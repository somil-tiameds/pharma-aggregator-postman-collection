$kind: http-request
description: Create a new role in the system.
url: "{{baseUrl}}/admin/roles"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "name": "SUPER_ADMIN"
    }
auth:
  type: bearer
  credentials:
    token: "{{supabase_service_role_api_key_06es}}"
settings: {}
order: 6000
