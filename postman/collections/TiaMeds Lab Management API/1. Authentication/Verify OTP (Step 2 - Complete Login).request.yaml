$kind: http-request
description: Step 2 of login. Validates OTP and issues JWT tokens as HttpOnly cookies (access_token, refresh_token). Both are set automatically by the server.
url: "{{baseUrl}}/auth/verify-otp"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "email": "somilm@tiameds.ai",
      "otp": "8992"
    }
settings: {}
examples: ./.resources/Verify OTP (Step 2 - Complete Login).resources/examples
order: 3000
