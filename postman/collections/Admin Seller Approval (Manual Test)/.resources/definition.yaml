$kind: collection
description: Manually approve/reject/correction a TempSeller registration for local testing, bypassing the (nonexistent) admin UI.
variables:
  baseUrl: http://localhost:8080/api/v1
  tempSellerId: "238"
